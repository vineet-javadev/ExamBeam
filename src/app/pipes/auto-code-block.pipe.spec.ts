import { AutoCodeBlockPipe } from './auto-code-block.pipe';

describe('AutoCodeBlockPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoCodeBlockPipe();
    expect(pipe).toBeTruthy();
  });
});
